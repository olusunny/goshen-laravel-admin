<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex,nofollow">
    <title>Goshen Retreat Unavailable</title>
    <style>
        :root {
            color-scheme: light;
            --ink: #0c2230;
            --muted: #667085;
            --gold: #f9b21d;
            --wash: #f3f8fb;
            --card: #ffffff;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            display: grid;
            place-items: center;
            padding: 24px;
            font-family: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            color: var(--ink);
            background:
                radial-gradient(circle at 18% 10%, rgba(249, 178, 29, 0.2), transparent 28%),
                radial-gradient(circle at 88% 16%, rgba(12, 34, 48, 0.12), transparent 30%),
                var(--wash);
        }

        .shell {
            width: min(520px, 100%);
            padding: 30px;
            border-radius: 30px;
            background: var(--card);
            box-shadow: 0 30px 70px rgba(12, 34, 48, 0.13);
            text-align: center;
        }

        .mark {
            width: 76px;
            height: 76px;
            margin: 0 auto 22px;
            display: grid;
            place-items: center;
            border-radius: 24px;
            background: linear-gradient(145deg, var(--ink), #123f4a);
            color: var(--gold);
            font-size: 36px;
            box-shadow: 0 20px 44px rgba(12, 34, 48, 0.22);
        }

        h1 {
            margin: 0 0 12px;
            font-size: clamp(28px, 7vw, 42px);
            line-height: 1.05;
            letter-spacing: 0;
        }

        p {
            margin: 0;
            color: var(--muted);
            font-size: 17px;
            line-height: 1.65;
        }

        .button {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-height: 52px;
            margin-top: 26px;
            padding: 0 24px;
            border-radius: 999px;
            color: var(--ink);
            background: var(--gold);
            font-weight: 800;
            text-decoration: none;
            box-shadow: 0 14px 30px rgba(249, 178, 29, 0.32);
        }
    </style>
</head>
<body>
    <main class="shell" role="main">
        <div class="mark" aria-hidden="true">✦</div>
        <h1>Goshen Retreat is currently unavailable</h1>
        <p>
            Registration is not open from the mobile web app right now.
            Please check back later or contact the church office for help.
        </p>
        <a class="button" href="/admin">Go to admin</a>
    </main>
</body>
</html>
